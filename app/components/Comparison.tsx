import PlanComparison from "./PlanComparison";

/** Back-compat default export for existing plans page imports. */
export default function Comparison() {
  return <PlanComparison />;
}

export { PlanComparison };
